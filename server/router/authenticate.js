const bcrypt=require('bcrypt');

const authenticate=(req,res,next)=>{next()}//todo: a lot of work
const register=(req,res,next)=>{next()}//todo: a lot of work

module.exports={authenticate,register}

